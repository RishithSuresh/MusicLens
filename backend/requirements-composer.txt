# Optional dependencies for the AI Music Compositor feature.
# Install only if you want the /compose endpoint enabled.
#
# music21 is required to render MIDI output.
# mido is required for MIDI parsing during audio synthesis.
# pydub is required for MP3 encoding.
# The compositor always works without LangGraph/LangChain by using the
# deterministic procedural engine. Install LLM packages separately only after
# security review if you need OPENAI_API_KEY-powered prompt interpretation.
music21==9.1.0
mido==1.3.3
pydub==0.25.1
python-dotenv==1.0.1
requests==2.32.3
